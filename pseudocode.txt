*** Pseudocode ***

**createPlayer function **
1   -   Three parameters, two for checking X and O, then one for checking the board
2   -   Create two constants that define each player (X & O), board variable should have been initialised somewhere after gameBoard function
3   -   Check if the cell at (x, y) on the board is empty
4   -   If empty, update the cell with the player's symbol
5   -   Return success or failure of the move

function createPlayer(player) {
    return player; // Return just the symbol
}

Change how players are created

Issues and Fixes
Updating Current Player: In your makeMove function, you are updating currentPlayer, but you’re not reflecting this change in the gameInstance object that's used to call updateTurn.

Displaying Correct Player: In your updateTurn function, the parameter currentPlayer should be a string like 'X' or 'O', but you are currently passing the player object directly.

Changes to Implement
Update the makeMove function to ensure that you're calling updateTurn with the correct symbol.

Modify the currentPlayer reference in the gameController to return the correct symbol when required.

Here’s how you can update your code:

Update the makeMove Function
Instead of returning the player object from createPlayer, return the symbol directly. Then, make sure you pass the right value to updateTurn

function gameController(board) {
    let X = createPlayer('X');
    let O = createPlayer('O');
    let currentPlayer = X; // Use symbols directly
    let gameOver = false;

    const makeMove = (row, column) => {
        // ... existing logic
        currentPlayer = currentPlayer === X ? O : X;
        updateTurn(currentPlayer); // Now this works correctly
        return true;
    };
    // ... remaining gameController logic
}

// In displayGame:
const renderGame = () => {
    // ... existing logic
    updateTurn(currentPlayer); // Make sure this is correct
};
