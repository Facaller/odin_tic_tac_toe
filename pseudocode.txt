*** Pseudocode ***

**createPlayer function **
1   -   Three parameters, two for checking X and O, then one for checking the board
2   -   Create two constants that define each player (X & O), board variable should have been initialised somewhere after gameBoard function
3   -   Check if the cell at (x, y) on the board is empty
4   -   If empty, update the cell with the player's symbol
5   -   Return success or failure of the move

Check Game Status: Ensure that the method is triggered only when a game is over. If gameOver is false, there’s no need to reset the game.

Reset Board State: Call the resetBoard method from the board instance to clear the board's state.

Reinitialize Players: Reset the currentPlayer to the starting player (usually 'X').

Reset Game Over Flag: Set gameOver back to false to allow new moves.

Restart Game Logic: After resetting, you might want to invoke nextTurn() or another method to prepare the game for a new round.

User Interaction: If you want to allow users to choose to play again, consider how you’ll handle this without using prompts. You might replace that part with a simple button click that triggers the reset logic.

Update the Display: Make sure to update the UI after resetting the game state so that players see the cleared board.